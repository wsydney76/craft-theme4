{% set entries = query.all %}
{% if entries %}
    <div class="container-{{ theme.pageWidth }} flex space-x-4 mt-16">
        {% for entry in entries %}
            <a class="btn border border-gray-900 text-gray-900" href="{{ entry.url }}">{{ 'Topic'|t }}: {{ entry.title }}</a>
        {% endfor %}
    </div>
{% endif %}
