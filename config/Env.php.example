<?php

namespace config;

class Env extends \modules\main\Env
{
    // This file is a replacement for the usual .env mechanism, avoiding some issues with thread safety on Windows10/Apache/mod_php
    // Like .env, it must not be committed to git.

    // replace getenv('VAR') with Env::VAR
    // For use in the Craft CP define aliases in config/general.php
    // like  '@SYSTEM_NAME' => Env::SYSTEM_NAME

    // dev/staging/production

    // php craft blitz/cache/warm
    const ENVIRONMENT = 'dev';

    const APP_ID = 'Craft_Theme';
    const SECURITY_KEY = 'eJXaXN6tDnPcd60PUOBeNl7pCuqNJLWX';

    const DB_DRIVER = 'mysql';
    const DB_SERVER = 'localhost';
    const DB_PORT = '3306';
    const DB_DATABASE = '';
    const DB_SCHEMA = ''; // Postgres only
    const DB_TABLEPREFIX = '';

    const DB_USER = 'root';
    const DB_PASSWORD = '';

    const DEFAULT_SITE_URL = "";

    const CP_VARIABLES = [
        'SYSTEM_NAME' => '',
        'SYSTEM_TIMEZONE' => 'Europe/Berlin',

        'EMAIL_ADDRESS' => 'support@example.com',
        'EMAIL_SENDER' => 'Support Team',

        'SMTP_HOST' => '',
        'SMTP_PORT' => '2525',
        'SMTP_AUTHENTICATION' => '1',
        'SMTP_USER' => '',
        'SMTP_PASSWORD' => '',
        'SMTP_ENCRYPTION_METHOD' => 'tls',

        'GOOGLE_API_KEY' => '',

    ];



}
